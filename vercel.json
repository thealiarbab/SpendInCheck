{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["server/**", "templates/**", "static/**"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "app.py" }]
}
